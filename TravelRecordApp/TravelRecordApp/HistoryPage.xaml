<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TravelRecordApp.HistoryPage">
    <ListView x:Name="postListView" ItemSelected="PostListView_ItemSelected">
        <!-- 2step to apply data binding: Set the actual binding to a property of the source (in this case, that property is Expierence) -->
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Expierience}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>